# 文档调整记录

## 2026-05-07

- 更新根目录 `README.md` 中的 nRF5340 固件编译命令，统一使用：
  `west build -p always -b pencilv/nrf5340/cpuapp -s . -o=-j8 -- -DBOARD_ROOT=.`
- 在根目录 `README.md` 中补充烧录步骤，明确从 `pencilv_stage_tflite_end2end\` 目录执行 `west flash`。
- 保留模型转换、校准数据重建和产物输出位置说明，便于后续自查和留档。
